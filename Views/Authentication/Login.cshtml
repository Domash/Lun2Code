@{
    Layout = "_LoginLayout";
}


@foreach (var item in Model)
{
    <a class="btn btn-primary login-button" asp-controller="Authentication" asp-action="SignIn" asp-route-provider="@item">@item</a>
}